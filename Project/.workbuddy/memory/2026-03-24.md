# 2026-03-24 工作日志

## 完成的工作

### Godot 项目 Excel/CSV 转 JSON 配置优化方案
- 设计了 JSON 数据结构（包含 `_metadata`、`cardInfo` 和 `eventInfo` 三部分）
- 创建了 `cardData.json` 配置文件：
  - cardInfo: 28 条记录
  - eventInfo: 2 条记录
- 编写了 `CardDataJSON.gd` 加载器，支持：
  - 自动类型转换（整数、字符串）
  - 便捷 API：`get_item()`, `get_items_by_class()`, `has_item()` 等
- 创建了 `tools/excel_to_json.py` 转换工具（推荐）：
  - 直接读取 Excel（.xlsx 和 .xls 都支持）
  - 自动类型转换
  - 一键转换所有配置
- 创建了 `tools/csv_to_json.py` 作为备选方案

### 基础事件面板需求分析（16:55）
- 读取了需求文档 `H:/文案资料/需求文档/基础事件面板.docx`
- 分析结论：
  - `event/event.tscn` 已有UI骨架（background/title/descriptionText/slot/Panel/Button），**但完全没有脚本**
  - 需新建：`event/event.gd`（面板主逻辑）、`event/card_slot.gd`（卡槽拖放接收）
  - 需修改：`event/event.tscn`（挂脚本+卡槽节点改造）、`card_table_main`（加测试按钮）、`cardData.json`（eventInfo card_1~6数据补充）
  - card_1~card_6 目前在 eventInfo 中全为0，需要在 eventInfo.xls 中配置真实数据再重新转换
  - result格式：`1_102_1` = 增加卡牌index为102的卡1张
  - 开发顺序：数据→卡槽→面板逻辑→牌桌测试按钮

### 基础事件面板代码实现（17:07）
- 新建 `event/card_slot.gd`：卡槽拖放接收器，加入 cardDropable 组，实现 `add_card()` 接口，支持类型检查（required_card_type）
- 新建 `event/event.gd`：面板主逻辑，含卡槽初始化/动态显示、条件检查、result解析执行、卡牌消耗
- 改造 `event/event.tscn`：6个卡槽从 ColorRect 改为 Control+CardSlot 结构（BgRect+CardAnchor），根节点挂载 event.gd
- 修改 `cards/card_table_main.gd`：加入 `open_event_panel_by_id()`、`add_new_card_by_name()` 等接口
- 修改 `cards/card_table_main.tscn`：新增「测试事件面板」按钮，连接到 `_on_test_event_button_pressed()`
- 二次修复卡牌无法放入卡槽（17:27）：
  - card.gd _process 中 whichDeckMouseIn 不清空导致残留，改为每帧先重置为 null
  - card.gd _on_button_button_up 末尾改为：判断 cardCurrentState != hanging 才写 following，用完后清空 whichDeckMouseIn
  - card_slot.gd _place_card 改为同步立即设置 hanging 状态（而不是在 call_deferred 里设置），确保在 _on_button_button_up 返回前状态已是 hanging
  - card_table_main 加 F2 快捷键诊断：打印所有 cardDropable 节点的 global_rect，用于运行时排查卡槽检测问题

- 三次修复（17:32）—— 找到真正根因：
  - 根因：grid_snap_table.gd 的 _on_mouse_exited 会在鼠标离开牌桌时把卡牌的 whichDeckMouseIn 清空
  - 拖卡从牌桌→事件面板时必然经过牌桌边缘，触发 mouse_exited，把刚检测到的卡槽引用清掉，导致松手时走回 preDeck.add_card
  - 修复：grid_snap_table.gd 的 _on_mouse_entered/_on_mouse_exited 改为空实现（pass），whichDeckMouseIn 完全由 card.gd _process 每帧实时检测

- 卡槽放入成功确认（17:50）：日志显示 state=4(hanging)、reparent 完成、parent=Slot1，卡牌放入正常

- 事件逻辑重构（17:50）：
  - 原逻辑：打开面板时固定 eventId，卡槽类型已确定
  - 新逻辑：面板打开时显示空白 + 一个"触发槽"，玩家拖任意卡入触发槽，根据卡的 eventId 动态加载事件（标题/描述/条件卡槽）
  - event.tscn 新增 TriggerSlot 节点（slot_index=-1，位于 slot 面板内，接受任意卡）
  - event.gd 重写：EventState 加 IDLE 状态，_on_trigger_card_placed 读 eventId 调 _load_event，_reset_to_idle 在触发卡移除时重置
  - card_table_main.gd 测试按钮改为调用 open_panel() 而非 open_event_by_id(2000)

- 修复触发槽打开面板不可见（18:04）：
  - 根因1：event.gd 里 trigger_slot 路径写成 $TriggerSlot，但节点实际是 $slot/TriggerSlot
  - 根因2：_collect_slots 把 TriggerSlot 也收进 slots 数组，被 _set_all_slots_visible(false) 隐藏
  - 修复：路径改为 $slot/TriggerSlot；_collect_slots 过滤 slot_index < 0 的节点；_reset_to_idle 末尾强制 trigger_slot.visible = true

- 修复从卡槽拖出时卡牌残留（18:17）：
  - 根因1：card_slot.remove_card() 判断 removed.get_parent()==self 可能失败，导致 remove_child 不执行
  - 根因2：card.gd 拖出逻辑在 remove_card() 之后还手动 remove_child(self)，与 remove_card() 内部冲突
  - 修复：card_slot.remove_card() 无论父节点是谁都执行摘出+重置锚点；card.gd 拖出逻辑去掉手动 remove_child

- 修复拖出后被 _do_reparent_card 重新塞回卡槽（18:38）：
  - 根因1：_place_card 里用 call_deferred(_do_reparent_card)，在拖出操作完成后下一帧仍执行，把已拖走的卡强行 add_child 回卡槽
  - 根因2：remove_card() 触发 card_removed 信号 → _on_trigger_card_removed → _reset_to_idle，误重置整个面板
  - 修复1：_do_reparent_card 增加守卫条件：若 preDeck != self 或 state==dragging 则跳过
  - 修复2：remove_card() 增加 silent 参数，card.gd 拖出时传 silent=true，不触发 card_removed 信号

- 五项修复与优化（18:09）：
  1. 修复增加卡牌不到牌桌：_add_single_card 坐标判断用 get_global_rect().get_center() 替代错误的 size/2
  2. hanging 状态禁点击 + 从卡槽拖出：card.gd _on_button_button_down 增加 hanging 分支，remove_card 通知卡槽、reparent 到 VfSlayer 继续拖动
  3. 确定后自动关闭：event.gd _on_confirm_pressed 末尾 await 0.8 秒后调 close_panel()
  4. 右上角关闭按钮：event.tscn 增加 CloseButton(✕)，event.gd 增加 _on_close_pressed()
  5. 松手离开卡槽回牌桌：card.gd _on_button_button_up 若 handled=false 则找 cardTable 组兜底 add_card
  - 根本原因：card.gd _on_button_button_up 末尾 `cardCurrentState = following` 会覆盖 add_card 里的状态设置
  - 解决方案：用 call_deferred("_do_attach_card") 延迟执行 reparent，在下一帧调用栈结束后再改状态
  - 卡牌放入后通过 reparent 到 CardSlot 节点下 + 全屏锚点（anchor 0-1）填满卡槽，不再依赖 follow_target/Marker2D
  - 放入后设为 hanging 状态（_process 不处理），防止弹簧物理移动卡牌
  - CardAnchor (Marker2D) 节点已删除，不再需要
  - 给 Slot 节点加了 clip_contents=true 和 mouse_filter=0，BgRect/SlotIcon 加 mouse_filter=2（ignore）

- 修复从卡槽拖出后无法放回牌桌（18:50）：
  - 根因1：幽灵副本（dup，state=vfs）也带 $Button，点击/松手事件被 dup 吸走，dup 没有 preDeck 导致打印"一般由于点击太快导致"，真实卡牌的松手逻辑被干扰
  - 根因2：卡牌从卡槽拖出后 global_position 仍在事件面板位置（约485,38），GridSnapTable._find_available_anchor 只搜索牌桌范围内的格子，全部过滤掉返回 -99999，place_card 走回退到原位流程（还是事件面板上）
  - 修复1：card.gd _on_button_button_down 和 _on_button_button_up 开头加 vfs 守卫（直接 return），彻底阻止幽灵副本响应按钮事件
  - 修复2：card.gd _on_button_button_up 松手时，若目标牌桌（GridSnapTable）的 global_rect 不包含卡牌当前位置，先把 global_position 移到牌桌 get_global_rect().get_center()，再调 add_card，确保 place_card 能找到合法锚点

- 修复 reparent 后 button_up 信号丢失导致卡牌吸附鼠标（18:50）：
  - 根因：hanging 分支 button_down 时把卡牌 reparent 到 VfSlayer，Button 的按压状态随父节点切换丢失，之后鼠标松开不会再触发 button_up 信号
  - 修复：card.gd 增加 _input 方法，在 dragging 状态下监听 MOUSE_BUTTON_LEFT released 事件，触发 _on_button_button_up 逻辑
  - 用 _releasing: bool 标志防止 _input 和 button_up 信号双重触发
  - _on_button_button_up 开头加 _releasing 防重入检查，各退出分支都重置该标志

- 修复多张同名卡都响应松手 + 防重入逻辑反了（19:04）：
  - 根因1：_releasing=true 先设置再调函数，函数内 if _releasing: return 直接退出，松手逻辑完全没执行
  - 根因2：多张同名卡（如3张ice）都处于 dragging 状态，_input 全部触发
  - 修复：废弃 _releasing 方案，改用 static var _current_dragger: card 全局唯一拖动者
  - button_down 时（hanging/following/旧deck三处）设置 _current_dragger = self
  - _input 里检查 _current_dragger != null and != self 时跳过，确保只有注册者处理松手
  - 把松手逻辑提取为 _do_release()，_input 和 button_up 信号共用
  - _do_release 开头清空 _current_dragger，防止同一张卡的 button_up 信号再重复触发

- 修复松手后卡牌不返回牌桌（19:10）：
  - 根因：兜底代码用 get_tree().get_nodes_in_group("cardTable") 找牌桌，但 CardTableMain/GridSnapTable 根本没有加入 "cardTable" 组，只加入了 "cardDropable" 组
  - 结果：tables.size()==0，兜底走不进去，最终 cardCurrentState=following 但 parent 还在 VfSlayer，卡牌漂在原地
  - 修复：兜底代码改为遍历 "cardDropable" 组，过滤出 GridSnapTable 类型的节点作为目标牌桌

- 修复 preDeck 路径松手不吸附锚点（19:18）：
  - release_card 时会把 global_position 存到 meta _last_valid_position（即拖起前的格子位置）
  - preDeck.add_card 之前先把 global_position 恢复为 _last_valid_position，place_card 就能找到原格子的锚点并吸附回去

- 修复从卡槽拖出松手停留问题（19:46）：
  - 日志显示"兜底失败：cardDropable 组里找不到 GridSnapTable"
  - 根因：card_table_main._ready 重写了父类 _ready 但没调 super._ready()，GridSnapTable._ready 里的 add_to_group("cardDropable") 从未执行
  - 修复：card_table_main._ready 开头加 super._ready()
  - 附带修复：旧 deck 模式 update_weight 调用加安全检查，避免父节点不是 deck 时崩溃
