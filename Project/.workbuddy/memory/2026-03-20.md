# 2026-03-20 工作日志

## 完成的任务

### 清理冗余脚本引用
- 删除了 `card_stack_manager.gd`, `independent_card_manager.gd`, `card_area_manager.gd` 等冗余脚本
- 更新了以下文件中对这些已删除脚本的引用：
  - `cards/card_table.tscn` - 移除了 CardStackManager 节点和脚本引用
  - `cards/card_table_main.gd` - 移除了 stack_manager 相关代码
  - `cards/card_table_with_stacking.gd` - 移除了 CardStackManager 依赖
  - `cards/draggable_card_with_stacking.gd` - 移除了 CardStackManager 调用
  - `cards/draggable_card_with_smart_stacking.gd` - 移除了 CardStackManager 调用

### 当前项目架构
- 卡牌基类：`cards/card.gd` - 处理绘制、拖动、点击、堆叠核心逻辑
- 牌桌类：`deck/deck.gd` - 管理卡牌添加、同名堆叠、不同名1/4偏移显示
- 测试场景：`test_stacking_scene.gd` - 测试堆叠功能

### 堆叠逻辑位置
- 同名卡牌完全重合堆叠：`deck.gd` 中的 `_try_stack_same_name` 方法
- 不同名卡牌1/4偏移：`deck.gd` 中的 `_calculate_stack_offset` 方法
- 卡牌堆叠判断：`card.gd` 中的 `canStackWith` 和 `doStack` 方法
